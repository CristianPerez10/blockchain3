import 'package:flutter/material.dart';

class SubastasPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Center(child: Text("Sala de subastas"))),
      body: Center(child: Text("Creacion, participacion y visualizacion de las salas de subastas")),
    );
  }
}