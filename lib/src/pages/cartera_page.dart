import 'package:flutter/material.dart';

class CarteraPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Center(child: Text("Cartera"))),
      body: Center(child: Text("Cartera del usuario")),
    );
  }
}